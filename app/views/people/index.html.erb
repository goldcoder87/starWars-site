<h1>LIST OF CHARACTERS</h1>

<ul>
<% @person.each do |person| %>

    <%= image_tag person.image.url(:thumb) %>

  <li>
    <%= link_to person.name, person_path(person) %>
  </li>
  <% end %>
</ul>

<%= link_to "Add a new Character", new_person_path %>
